discogs {
  api = "127.0.0.1:8080"
  domain = "127.0.0.1:8080"
  scheme = "http"

  # this should be added by the library user in `application.conf`
  consumer {
    app-name: "mock-app",
    app-version: "0.0.1",
    key: "mock-consumer-key",
    secret: "mock-consumer-secret"
  }

  logger {
    name = "discogs4s-test"
  }

}