user-agent {
    app-name = ${?APP_NAME}
    app-version = ${?APP_VERSION}
    app-url = ${?APP_URL}
}

discogs {
  scheme = "https"
  domain = "discogs.com"
  api = "api.discogs.com"
}

logger {
    name = "discogs4s"
}
