# this should be added by the library user in `application.conf`
user-agent {
    app-name = "mock-app"
    app-version = "0.0.1"
}

discogs {
  base-uri = "http://127.0.0.1:8080"
  api-uri = "http://127.0.0.1:8080"

  # this should be added by the library user in `application.conf`
  consumer {
    key: "mock-consumer-key",
    secret: "mock-consumer-secret"
  }

  username: "test-user"
  access-token: "SAMPLE_ACCESS_TOKEN"

  logger {
    name = "discogs4s-test"
  }
}